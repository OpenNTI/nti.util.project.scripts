{
	"name": "@nti/workspace",
	"version": "1.0.0",
	"workspaces": [
		"./app/*",
		"./lib/*",
		"./style/*",
		"./util/*",
		"./scripts/packages/*",
		"./web/*",
		"!./server"
	],
	"scripts": {
		"#1": "npm7 currently has a bug where it will install the GIT version of deps even when its linked in the workspace...",
		"clean-dupes": "node -e \"require('glob').sync('./!(node_modules)/**/node_modules/').filter((x, i, a) => !a.slice(0,i).find(y => x.startsWith(y))).forEach(x => fs.promises.rmdir(x, {recursive:true}));\"",
		"#2": "Lerna isn't compatible with this workspace layout, so we just install it by itself",
		"install-lerna": "cd scripts; npm install",
		"install-server": "cd server; npm install",
		"postinstall": "npm run clean-dupes; npm run install-lerna; npm run install-server"
	},
	"engines": {
		"npm": ">=7.0.0",
		"node": ">=14.0.0"
	},
	"dependencies": {
		"@nti/eslint-config-app-scripts": "*",
		"@nti/eslint-config-lib-scripts": "*",
		"@nti/stylelint-config-standard": "*"
	}
}
